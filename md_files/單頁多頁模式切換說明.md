# 單頁/多頁模式切換說明

## 📋 概述

本專案支援**單頁模式**和**多頁模式**的靈活切換，讓您可以根據需求快速調整應用的佈局結構和登入後的首頁。

- **單頁模式**：適合只需要一個編輯頁面的情境，隱藏側邊欄和導航選單
- **多頁模式**：適合需要多個功能頁面的情境，顯示側邊欄和導航選單

---

## 🎯 目前設定

目前專案設定為 **單頁模式（Accounts 帳戶資料頁）**：

- ✅ 登入後直接進入「學生帳戶資料」頁面
- ✅ 隱藏左側側邊欄
- ✅ 隱藏漢堡按鈕和麵包屑導航
- ✅ 保留用戶頭像和登出功能

---

## 🔄 如何切換模式和首頁

### 方法一：修改配置檔案（推薦）

#### 步驟 1: 設定側邊欄顯示/隱藏

打開 `src/config/layouts.js`，修改以下配置：

```javascript
const defaultSettings = {
  // ... 其他配置 ...
  showSidebar: false, // true = 多頁模式 / false = 單頁模式
  showHamburger: false // true = 顯示漢堡按鈕 / false = 隱藏
}
```

#### 步驟 2: 設定登入後的首頁

需要修改 **三個檔案**：

##### 2.1 設定根路徑重定向

打開 `src/router/index.js`，找到第 50 行左右：

```javascript
{
  path: "/",
  component: Layouts,
  redirect: "/accounts/index",    // ← 修改這裡
  children: [
    // ...
  ]
}
```

**可選擇的路徑：**

- `/accounts/index` - 學生帳戶資料頁（學生自填資料）
- `/single-fruit/index` - 單一水果編輯頁（單筆資料編輯）
- `/fruits/index` - 水果管理頁（列表 CRUD）
- `/dashboard` - 儀表板首頁（多頁模式推薦）

##### 2.2 設定路由守衛跳轉

打開 `src/router/permission.js`，找到第 28-30 行：

```javascript
// 如果已经登录，并准备进入 Login 页面，则重定向到 Accounts 頁面
if (to.path === "/login") {
  return next({ path: "/accounts/index" }) // ← 修改這裡
}
```

##### 2.3 設定登入頁面跳轉 ⭐ 重要

打開 `src/views/login/index.vue`，找到第 78 行左右：

```javascript
/** 登录逻辑 */
const handleLogin = () => {
  loginFormRef.value?.validate((valid) => {
    if (valid) {
      loading.value = true
      useUserStore()
        .login(loginFormData)
        .then(() => {
          router.push({ path: "/accounts/index" }) // ← 修改這裡
        })
      // ...
    }
  })
}
```

**⚠️ 重要：** 這三個檔案中的路徑必須**完全一致**，否則登入後跳轉會不正確！

#### 步驟 3: 重新整理頁面

修改完成後，重新整理瀏覽器（Ctrl + F5），完成！

---

## 📋 常用配置範例

### 範例 1: 單頁模式 - Accounts（學生帳戶資料）

**適用情境：** 學生登入後填寫自己的帳戶資料

```javascript
// src/config/layouts.js
showSidebar: false,
showHamburger: false,

// src/router/index.js
redirect: "/accounts/index",

// src/router/permission.js
return next({ path: "/accounts/index" })
```

### 範例 2: 單頁模式 - Single Fruit（單一水果編輯）

**適用情境：** 專注於單一資料的編輯

```javascript
// src/config/layouts.js
showSidebar: false,
showHamburger: false,

// src/router/index.js
redirect: "/single-fruit/index",

// src/router/permission.js
return next({ path: "/single-fruit/index" })
```

### 範例 3: 多頁模式 - Dashboard（儀表板）

**適用情境：** 需要切換多個功能頁面

```javascript
// src/config/layouts.js
showSidebar: true,
showHamburger: true,

// src/router/index.js
redirect: "/dashboard",

// src/router/permission.js
return next({ path: "/dashboard" })
```

### 範例 4: 多頁模式 - Fruits（水果管理）

**適用情境：** 管理員需要列表 CRUD 功能

```javascript
// src/config/layouts.js
showSidebar: true,
showHamburger: true,

// src/router/index.js
redirect: "/fruits/index",

// src/router/permission.js
return next({ path: "/fruits/index" })
```

---

## 📁 需要修改的檔案清單

### ✅ 必須修改的檔案（4 個）

#### 1. `src/config/layouts.js`

**佈局配置檔案** - 控制側邊欄和漢堡按鈕的顯示

```javascript
const defaultSettings = {
  showSidebar: false, // 控制側邊欄顯示
  showHamburger: false // 控制漢堡按鈕顯示
}
```

#### 2. `src/router/index.js`

**路由配置檔案** - 控制根路徑 `/` 的重定向

```javascript
{
  path: "/",
  component: Layouts,
  redirect: "/accounts/index",  // ← 第 50 行，修改這裡
  children: [ /* ... */ ]
}
```

#### 3. `src/router/permission.js`

**路由守衛檔案** - 控制已登入用戶訪問登入頁時的跳轉

```javascript
// ← 第 28-30 行，修改這裡
if (to.path === "/login") {
  return next({ path: "/accounts/index" })
}
```

#### 4. `src/views/login/index.vue` ⭐ 最重要

**登入頁面組件** - 控制登入成功後的跳轉

```javascript
// ← 第 78 行，修改這裡
const handleLogin = () => {
  // ...
  .then(() => {
    router.push({ path: "/accounts/index" })
  })
}
```

### 📖 相關組件檔案（不需修改）

#### 4. `src/layouts/LeftMode.vue`

**左側佈局組件** - 實作側邊欄的顯示/隱藏邏輯

#### 5. `src/layouts/components/NavigationBar/index.vue`

**導航欄組件** - 實作漢堡按鈕的顯示/隱藏邏輯

---

## 🎨 模式對比

| 項目       | 單頁模式                 | 多頁模式       |
| ---------- | ------------------------ | -------------- |
| 側邊欄     | ❌ 隱藏                  | ✅ 顯示        |
| 漢堡按鈕   | ❌ 隱藏                  | ✅ 顯示        |
| 麵包屑     | ❌ 隱藏                  | ✅ 顯示        |
| 頁面切換   | ❌ 無                    | ✅ 可切換多頁  |
| 登入後首頁 | 單一頁面（自訂）         | Dashboard      |
| 適用情境   | 單一功能（填表、編輯等） | 多功能管理系統 |

## 📋 可用的頁面路徑

| 路徑                  | 頁面名稱     | 功能說明               | 適合模式 |
| --------------------- | ------------ | ---------------------- | -------- |
| `/accounts/index`     | 學生帳戶資料 | 學生填寫自己的帳戶資料 | 單頁     |
| `/single-fruit/index` | 單一水果編輯 | 單筆資料編輯           | 單頁     |
| `/fruits/index`       | 水果管理     | 列表 CRUD 管理         | 多頁     |
| `/dashboard`          | 儀表板       | 首頁總覽               | 多頁     |

---

## 🔧 技術細節

### 配置原理

本專案使用 Pinia Store 管理佈局配置，配置會自動同步到 localStorage。

```javascript
// src/config/layouts.js
export const layoutSettings = { ...defaultSettings, ...getConfigLayout() }
```

### 響應式控制

配置變更會自動觸發組件重新渲染：

```vue
<!-- 側邊欄 -->
<Sidebar v-if="showSidebar" class="sidebar-container" />

<!-- 漢堡按鈕 -->
<Hamburger v-if="showHamburger && (!isTop || isMobile)" ... />
```

### CSS 樣式適配

當隱藏側邊欄時，主要內容區域會自動調整：

```scss
.noSidebar {
  .main-container {
    margin-left: 0 !important; // 移除左側邊距
  }
  .fixed-header {
    width: 100% !important; // 標題欄佔滿寬度
  }
}
```

---

## 📝 常見問題

### Q1: 我需要修改幾個檔案？

**A:** 總共需要修改 **4 個檔案**：

1. `src/config/layouts.js` - 控制側邊欄顯示
2. `src/router/index.js` - 控制根路徑重定向
3. `src/router/permission.js` - 控制路由守衛跳轉
4. `src/views/login/index.vue` - **控制登入成功後的跳轉（最重要！）**

### Q2: 為什麼要修改三個路由相關的檔案？

**A:** 這三個檔案控制不同的跳轉時機：

- `router/index.js` - 控制訪問根路徑 `/` 時的跳轉
- `router/permission.js` - 控制已登入用戶訪問 `/login` 時的跳轉
- `views/login/index.vue` - **控制登入按鈕點擊後的跳轉（優先級最高！）**
- **⚠️ 這三個路徑必須完全一致**，否則登入後跳轉會不正確

### Q3: 修改配置後沒有生效？

**A:** 請確保：

1. **所有 4 個檔案都已儲存**（特別是 `login/index.vue`！）
2. 已重新建置並部署（`pnpm run deploy:gas`）
3. 在 GAS 網頁介面重新部署為新版本
4. 使用無痕模式或 Ctrl + Shift + R 清除快取
5. 路徑格式正確（例如：`/accounts/index` 而不是 `/accounts`）
6. 如果還是不行，檢查瀏覽器 Console 是否有錯誤訊息

### Q4: 如何清除 localStorage？

**A:** 在瀏覽器開發者工具中：

1. 按 F12 打開開發者工具
2. 進入 Application（應用程式）標籤
3. 左側選單找到 Local Storage
4. 點擊並刪除相關的儲存項目
5. 重新整理頁面

### Q5: 可以在執行時切換模式嗎？

**A:** 可以！配置是響應式的。您可以：

1. 在右上角的設定面板中調整（如果 `showSettings: true`）
2. 使用 Vue DevTools 直接修改 Store 的值
3. 修改配置檔案並重新整理

### Q6: 手機版會有影響嗎？

**A:** 不會！手機版有獨立的響應式設計：

- 單頁模式：內容全寬顯示
- 多頁模式：自動出現抽屜式側邊欄

### Q7: 如何新增自己的頁面作為首頁？

**A:**

1. 先在 `src/router/index.js` 中註冊您的路由
2. 然後按照本文件的步驟，將路徑改為您的路由路徑
3. 例如：新增 `/my-page/index` 作為首頁

---

## 🚀 快速切換指南

### 方法：一次修改完成（複製貼上法）

選擇您需要的情境，複製對應的程式碼，貼到指定的檔案中：

---

### 情境 1：學生帳戶資料頁（單頁模式）

**適用：** 學生登入後填寫自己的資料

```javascript
// ========== src/config/layouts.js ==========
const defaultSettings = {
  // ... 其他配置 ...
  showSidebar: false,
  showHamburger: false
}

// ========== src/router/index.js（第 50 行）==========
{
  path: "/",
  component: Layouts,
  redirect: "/accounts/index",
  // ...
}

// ========== src/router/permission.js（第 28-30 行）==========
if (to.path === "/login") {
  return next({ path: "/accounts/index" })
}

// ========== src/views/login/index.vue（第 78 行）==========
const handleLogin = () => {
  // ...
  useUserStore()
    .login(loginFormData)
    .then(() => {
      router.push({ path: "/accounts/index" })
    })
}
```

---

### 情境 2：水果編輯頁（單頁模式）

**適用：** 專注於單一資料編輯

```javascript
// ========== src/config/layouts.js ==========
const defaultSettings = {
  // ... 其他配置 ...
  showSidebar: false,
  showHamburger: false
}

// ========== src/router/index.js（第 50 行）==========
{
  path: "/",
  component: Layouts,
  redirect: "/single-fruit/index",
  // ...
}

// ========== src/router/permission.js（第 28-30 行）==========
if (to.path === "/login") {
  return next({ path: "/single-fruit/index" })
}
```

---

### 情境 3：多功能管理系統（多頁模式）

**適用：** 開發測試或管理員使用

```javascript
// ========== src/config/layouts.js ==========
const defaultSettings = {
  // ... 其他配置 ...
  showSidebar: true,
  showHamburger: true
}

// ========== src/router/index.js（第 50 行）==========
{
  path: "/",
  component: Layouts,
  redirect: "/dashboard",
  // ...
}

// ========== src/router/permission.js（第 28-30 行）==========
if (to.path === "/login") {
  return next({ path: "/dashboard" })
}
```

---

### 情境 4：水果管理列表（多頁模式）

**適用：** 需要 CRUD 列表管理

```javascript
// ========== src/config/layouts.js ==========
const defaultSettings = {
  // ... 其他配置 ...
  showSidebar: true,
  showHamburger: true
}

// ========== src/router/index.js（第 50 行）==========
{
  path: "/",
  component: Layouts,
  redirect: "/fruits/index",
  // ...
}

// ========== src/router/permission.js（第 28-30 行）==========
if (to.path === "/login") {
  return next({ path: "/fruits/index" })
}
```

---

## 📊 完整設定對照表

| 情境               | showSidebar | showHamburger | router/index.js       | router/permission.js  | login/index.vue       |
| ------------------ | ----------- | ------------- | --------------------- | --------------------- | --------------------- |
| 學生帳戶資料（單） | `false`     | `false`       | `/accounts/index`     | `/accounts/index`     | `/accounts/index`     |
| 水果編輯（單）     | `false`     | `false`       | `/single-fruit/index` | `/single-fruit/index` | `/single-fruit/index` |
| 儀表板（多）       | `true`      | `true`        | `/dashboard`          | `/dashboard`          | `/dashboard`          |
| 水果管理（多）     | `true`      | `true`        | `/fruits/index`       | `/fruits/index`       | `/fruits/index`       |

**⚠️ 重要提醒：** 同一列的所有路徑必須完全相同！

---

## 📞 技術支援

如有任何問題，請參考：

- [Accounts功能說明文件](./Accounts功能說明文件.md) - 學生帳戶資料模組
- [SingleFruit功能說明文件](./SingleFruit功能說明文件.md) - 單一編輯模組
- [Fruits功能說明文件](./Fruits功能說明文件.md) - 列表 CRUD 模組
- [專案重構說明](./專案重構說明.md) - 專案架構說明
- [快速開始](./快速開始.md) - 快速入門指南

---

**最後更新日期**: 2025-10-12  
**版本**: 2.0.0  
**更新內容**: 新增 Accounts 模組說明，完善配置檔案對照表
