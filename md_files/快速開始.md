# 🚀 快速開始指南

## 立即開始開發

### 1. 安裝依賴

```bash
# 安裝依賴
pnpm install
```

### 2. 部署到 GAS

本專案採用 **直接部署到 GAS 測試** 的開發方式，不使用本地開發伺服器。

```bash
# 建置並部署到 GAS
pnpm run deploy:gas
```

### 3. 在瀏覽器測試

部署完成後：

1. 開啟測試部署網址（`.../dev`）
2. 清除快取（Ctrl+Shift+R 或 Cmd+Shift+R）
3. 查看最新的修改結果
4. 確認功能正確無誤

### 4. 建立正式版本

測試確認無誤後：

1. 建立新的部署版本
2. 使用正式部署網址（`.../exec`）
3. 提供給實際使用者使用

### 5. 登入系統

使用 GAS 後端的登入系統：

- 帳號和密碼由 `gas/login.js` 中的邏輯決定
- 預設可參考該檔案的實作

### 6. 查看範例

登入後可以看到：

- **首頁**：Dashboard 主頁
- **試算表管理**：完整的 CRUD 範例

## 新增你的第一個模組

### 步驟 1：創建 API

建立 `src/api/my-feature/index.js`：

```javascript
import { callGasFunction } from "@/utils/gas-service"

export function getMyData() {
  return callGasFunction("getMyData")
}
```

### 步驟 2：創建頁面

建立 `src/views/my-feature/index.vue`：

```vue
<script setup>
import { ref, onMounted } from "vue"
import { ElMessage } from "element-plus"
import { getMyData } from "@/api/my-feature"

const data = ref([])
const loading = ref(false)

const loadData = async () => {
  loading.value = true
  try {
    const response = await getMyData()
    if (response.code === 0) {
      data.value = response.data
      ElMessage.success("載入成功")
    }
  } catch (error) {
    ElMessage.error("載入失敗：" + error.message)
  } finally {
    loading.value = false
  }
}

onMounted(() => {
  loadData()
})
</script>

<template>
  <div class="app-container">
    <el-card v-loading="loading">
      <h2>我的功能</h2>
      <!-- 你的內容 -->
    </el-card>
  </div>
</template>

<style scoped>
.app-container {
  padding: 20px;
}
</style>
```

### 步驟 3：新增路由

編輯 `src/router/index.js`，在 `constantRoutes` 陣列中新增：

```javascript
{
  path: "/my-feature",
  component: Layouts,
  redirect: "/my-feature/index",
  children: [
    {
      path: "index",
      component: () => import("@/views/my-feature/index.vue"),
      name: "MyFeature",
      meta: {
        title: "我的功能",
        svgIcon: "component"
      }
    }
  ]
}
```

### 步驟 4：創建 GAS 函數

建立 `gas/my-feature.js`：

```javascript
/**
 * 取得我的資料
 * @returns {Object} 回應物件
 */
// eslint-disable-next-line no-unused-vars
function getMyData() {
  try {
    // 你的 GAS 邏輯
    const data = ["真實資料1", "真實資料2"]

    return {
      code: 0,
      data: data,
      message: "成功"
    }
  } catch (error) {
    return {
      code: 1,
      data: null,
      message: "錯誤: " + error.message
    }
  }
}
```

### 步驟 5：部署測試

```bash
# 建置並部署到 GAS
pnpm run deploy:gas
```

部署完成後：

1. 在瀏覽器開啟測試部署網址（`.../dev`）
2. 清除快取（Ctrl+Shift+R）
3. 測試新功能
4. 如有修改，再次執行 `pnpm run deploy:gas` 並重新測試
5. 確認功能完全正確後，建立正式部署版本（`.../exec`）

## 常用指令

```bash
# 開發與部署
pnpm run deploy:gas       # 建置並部署到 GAS（主要開發指令）
pnpm run build:gas        # 僅建置 GAS 版本（不部署）

# 程式碼品質
pnpm lint                 # 格式化和檢查程式碼
pnpm test                 # 執行測試
```

**開發流程：**

1. 修改程式碼
2. 執行 `pnpm run deploy:gas`
3. 在瀏覽器開啟測試網址（`.../dev`）
4. 清除快取查看最新結果
5. 重複步驟 1-4 直到功能完成
6. 確認無誤後建立正式部署版本（`.../exec`）

## 可用的圖示

在路由 meta 中可以使用：

### Element Plus 圖示（elIcon）

```javascript
meta: {
  title: "標題",
  elIcon: "Document"  // Element Plus 圖示名稱
}
```

常用圖示：Document, Setting, User, Grid, Menu, Tickets

### SVG 圖示（svgIcon）

```javascript
meta: {
  title: "標題",
  svgIcon: "dashboard"  // src/icons/svg/ 中的檔案名稱
}
```

可用圖示：dashboard, component, menu, search, link, lock

## 試算表範例

參考 `src/views/spreadsheet/index.vue` 和 `gas/spreadsheet.js` 了解如何：

- 讀取 Google Spreadsheet 資料
- 新增、編輯、刪除資料列
- 處理多個工作表
- 錯誤處理和載入狀態

這是最佳實踐範例！

## 開發技巧

### 快速除錯

1. **前端除錯**

   - 使用瀏覽器開發者工具（F12）
   - 查看 Console 標籤的錯誤訊息
   - 使用 Network 標籤查看 API 呼叫

2. **GAS 後端除錯**

   - 在 GAS 編輯器中使用 `Logger.log()`
   - 查看 GAS 的執行記錄
   - 使用 try-catch 捕獲並記錄錯誤

3. **清除快取**
   - Windows/Linux：Ctrl + Shift + R
   - Mac：Cmd + Shift + R
   - 或在開發者工具中勾選「停用快取」

### 提高效率

1. **使用監控模式**

   - 開啟終端機，隨時準備執行 `pnpm run deploy:gas`
   - 修改存檔後立即部署
   - 在瀏覽器保持部署網址開啟

2. **批次修改**

   - 一次修改多個相關檔案
   - 統一部署測試，減少部署次數

3. **版本控制**
   - 使用 Git 追蹤修改
   - 重要功能完成後提交
   - 方便回溯問題

## 需要協助？

1. 查看 [專案重構說明.md](./專案重構說明.md) 了解詳細資訊
2. 查看 [GAS\_部署說明.md](./GAS_部署說明.md) 了解部署流程
3. 參考試算表管理範例的程式碼
4. 查看 GAS 編輯器的執行記錄除錯

## 下一步

- 📖 閱讀 [專案重構說明.md](./專案重構說明.md)
- 🔧 修改 Dashboard 顯示你的內容
- 🎨 自訂主題和佈局
- 📊 開發你的業務模組
- 🚀 熟悉部署流程

祝你開發順利！🎉
